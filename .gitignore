# Dependencies
node_modules/
bun.lock
package-lock.json

# macOS system files
.DS_Store

# Runtime state (SQLite)
ensemble.db
ensemble.db-shm
ensemble.db-wal

# Commands folder (generated symlinks, not part of repo)
commands/

# Pi runtime state — never publish (lives in ~/.pi/agent; these are insurance)
pi/auth.json
pi/models-store.json
pi/sessions/
pi/settings.json
